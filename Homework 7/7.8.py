'''
First, Start the process with both the sides the header and the trailer sentinels
Then for the header, use the link hopping to the next link every time
Whereas for the trailer, use the link hopping to the previous link every time
When two nodes from the header and trailer reaches a a same point then it is the middle
The running time is O(n) as it scans all the nodes
'''